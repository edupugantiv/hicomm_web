Key Screen 6: Group Page <br /><br />

<%=@group.name%> <br />
Scale: <%=@group.scale%> <br />
Contact: <br/> 
<%=@group.group_leader.name%> - Project Manager <br />
Email: <%=@group.group_leader.email%> <br />

Latitude: <%=@group.latitude%><br />
Longitude: <%=@group.longitude%><br />

<%= link_to 'Edit', edit_group_path(@group) %><br />
<%= link_to 'Back', groups_path %>

<%if can? :transfer_leadership, @group%> 
	<%=button_to "Manage Group", manage_group_path(@group), :method => 'get'%>
<%elsif can? :leave, @group%>
	<%=button_to "Leave Group", leave_group_path(@group), :method => 'put' %>
<% else %> 
	<%=button_to "Join Group", join_group_path(@group), :method => 'put' %>
<%end%>

Members: <br />
<%@members.each do |member| %> 
	<%=member.name%> <br />
<%end%>
